# Resume Parser (Streamlit + Ollama)

A simple web app that compares a resume against a job description
and returns a structured JSON analysis.

## Run locally
pip install -r requirements.txt
streamlit run app.py
